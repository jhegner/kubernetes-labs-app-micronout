name: 🚀 - CI Develop (merge)

on:
  push:
    branches:
      - develop

jobs:
  ci-feature:
    uses: jhegner/kubernetes-labs-ci-workflows/.github/workflows/1.1-reusable-develop.yml@main
    with:
      image-name: "kubernetes-labs-app-micronout"
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
      VULTR_API_KEY: ${{ secrets.VULTR_API_KEY }}