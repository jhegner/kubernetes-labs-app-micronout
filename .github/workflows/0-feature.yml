name: Reuso CI Feature

on:
  push:
    branches:
      - feature/**

jobs:
  ci-feature:
    uses: jhegner/kubernetes-labs-ci-workflows/.github/workflows/0-feature-java-mvn.yml@main
    with:
      project-name: kubernetes-labs-app-micronout            
    secrets:      
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SONAR_KEY: ${{ secrets.SONAR_KEY }}
